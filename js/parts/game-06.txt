  function render(){resize();gl.viewport(0,0,canvas.width,canvas.height);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);cameraShake*=.88;const camera=computeCamera();projection=M.persp((cameraMode===2?45:50)*Math.PI/180,canvas.width/canvas.height,.1,190);view=M.look(camera.eye,camera.target);vp=M.mul(projection,view);for(const o of statics)draw(o.mesh,o.p,o.r,o.s,o.color,o.alpha,o.unlit,o.rx);renderAnimatedStadium();drawPowerups();players.forEach(drawPlayer);drawBall();trails.forEach((trail)=>draw(meshes.sphere,[trail.x,trail.y,trail.z],0,[trail.size,trail.size,trail.size],trail.color,Math.min(.55,trail.life*1.4),1));particles.forEach((p)=>draw(meshes.cube,[p.x,p.y,p.z],0,[p.size,p.size,p.size],p.color,Math.min(1,p.life*1.8),1));if((frameCounter++&1)===0)updateMiniMap();}
  function frame(now){const dt=Math.min(.033,(now-last)/1000||.016);last=now;if(commentTimer>0){commentTimer-=dt;if(commentTimer<=0)$('commentary').classList.remove('show');}update(dt);render();window.__GAME_READY=true;requestAnimationFrame(frame);}

  $('startBtn').addEventListener('click',()=>startGame(false));$('restartBtn').addEventListener('click',()=>startGame(true));$('menuBtn').addEventListener('click',returnToMenu);$('resumeBtn').addEventListener('click',resumeGame);$('soundBtn').addEventListener('click',toggleMute);$('menuSoundBtn').addEventListener('click',toggleMute);
  if('serviceWorker' in navigator&&location.protocol.startsWith('http'))window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));
  if(new URLSearchParams(location.search).has('debug')){window.__GAME_TEST__={start:()=>startGame(false),step:(dt)=>update(dt),forceGoal:(team=0)=>{state='playing';scoreGoal(team);},getState:()=>state,getScore:()=>[homeScore,awayScore],setKey:(key,value)=>{keys[key]=value;},getMoveInput:()=>moveInput(),getKickoffTimer:()=>kickoffTimer,getGoalPause:()=>goalPause,getControlledPosition:()=>players[controlled]?{x:players[controlled].x,z:players[controlled].z,stamina:players[controlled].stamina}:null,getCanvas:()=>({width:canvas.width,height:canvas.height,cssWidth:canvas.clientWidth,cssHeight:canvas.clientHeight}),isMobileProfile:()=>mobileProfile};}
  setGameActiveUI(false);updateSoundButtons();updateHUD();requestAnimationFrame(frame);
})();
